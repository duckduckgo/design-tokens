import { DesignToken } from 'style-dictionary/types';

const card: DesignToken = {
    $type: 'card',
    undo: 'none',
    card: {
        backgroundColor: {
            $value: 'red',
            dark: {
                $value: 'red',
            },
        },
        borderRadius: {
            $value: 'red',
            dark: {
                $value: 'red',
            },
        },
    },
};

export default card;
