import { DesignToken } from 'style-dictionary/types';

const space: DesignToken = {
    $type: 'dimension',

    space: {
        pxInRem: {
            $value: '16px',
        },
    },
};

export default space;
